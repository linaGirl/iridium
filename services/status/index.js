

	// the status service hosts global application settings, its a driectory for whatever
	// the app requires a central storage. all data gets lost on restart. the service
	// can be run as a replica set ( one master, n failovers ).